{
  "openapi": "3.0.0",
  "info": {
    "title": "BushBuddy API Gateway",
    "version": "1.0.0",
    "description": "## API documentation for BushBuddy Wildlife Detection App\nN.B the API makes use of tokens stored in HTTP-only cookies to handle authenticated requests. Requests made to protected endpoints **must** include `credentials: include` in the fetch config."
  },
  "components": {
      "securitySchemes": {
        "jwtCookieAuth": {
          "type": "apiKey",
          "in": "cookie",
          "name": "token",
          "description": "JWT Authentication using HTTP-only cookie"
        }
      }
    },
    "security": [
      { "jwtCookieAuth": [] }
    ],
  "apis": [
    { "url": "http://localhost:4001/docs-json", "name": "Authentication Service" },
    { "url": "http://localhost:4002/docs-json", "name": "Discovery Service" }
  ]
}
